"""
Main entry point for CLI package.

This allows running the CLI as:
    python -m presentation.cli [command] [options]
"""

from presentation.cli.main import main

if __name__ == '__main__':
    main()
